# Fase 1 -- verificado contra imports reales del repo (grep, no supuesto):
# core/scoring.py -> numpy. ingestion/adapters.py -> pandas (top-level),
# websockets (lazy, DerivAdapter._default_connector). ingestion/gdelt.py
# -> httpx (async nativo -- no requests, que es bloqueante y ya se
# eliminó esa trampa en fetch_async()/AdapterChain). tests -> pytest,
# pytest-asyncio. NO incluye PyTorch/ML (Decisión #7: sin ML en F1) ni
# polars (gdelt_foundation.py lo usa; se evaluó no usarlo acá para no
# sumar una dependencia pesada solo por el parseo de 8 columnas -- ver
# ingestion/gdelt.py, parseo con str.split() puro).
#
# pandas ES LA ÚNICA DEPENDENCIA PINEADA, y con motivo concreto:
# ingestion/adapters.py transporta metadata de calidad por `df.attrs`, que
# pandas documenta como API EXPERIMENTAL. El diseño depende de su
# comportamiento exacto -- medido en 3.0.5, no supuesto: attrs sobrevive
# copy(), sort_values() y concat(), y se pierde en merge(). Sin el pin, una
# resolución distinta en CI podría cambiar ese comportamiento sin que nadie
# toque una línea de código. El rango es amplio a propósito (no ==): fija el
# límite donde una major nueva podría romper el contrato, no una versión
# exacta que obligue a actualizarla a mano cada mes.
# numpy, httpx, websockets y pytest NO se pinean: nada del contrato depende
# de su versión, y pinear de más obliga a mantener números sin motivo.
#
# polars ENTRA con el Brief H1-A (25-sep-2026): ingestion/velas.py::leer_velas()
# devuelve un pl.DataFrame, porque el lector del experimento lo usa. La
# decisión de arriba (no usarlo para el parseo de GDELT) sigue en pie: es otra
# pieza. Se pinea a la major 1 porque el contrato de leer_velas() se escribió y
# se midió contra 1.44.2 -- en particular, que `strict=True` convierte tipos en
# silencio y por eso los tipos se validan antes, fila por fila.
numpy
pandas>=2.2,<4
polars>=1.0,<2
websockets
httpx
pytest
pytest-asyncio
